
/* sol by: uj
*  time : Tuesday 21 January 2025 03:31:36 PM IST
*  rating :
*  problemNum : minimizingCoins 
*  tags :
*/
#include<bits/stdc++.h>
#include <climits>
using namespace std;
typedef long long ll;
const ll MOD = 1e9 + 7;
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    ll n,x; cin >> n >> x;
    vector<ll> coins(n);
    for(ll i = 0; i < n; i++) cin >> coins[i];
    vector<ll> dp(x, INT_MAX);
}

